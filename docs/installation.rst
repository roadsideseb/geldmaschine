============
Installation
============

At the command line::

    $ easy_install geldmaschine

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv geldmaschine
    $ pip install geldmaschine